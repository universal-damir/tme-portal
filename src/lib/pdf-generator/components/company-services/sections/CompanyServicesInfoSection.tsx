import React from 'react';
import { View } from '@react-pdf/renderer';
import type { PDFComponentProps } from '../../../types';

// CompanyServicesInfoSection - Info section for company services (contacts moved to MeetTheTeamPage)
export const CompanyServicesInfoSection: React.FC<PDFComponentProps> = ({ data }) => {
  return (
    <View style={{ marginTop: 20, marginBottom: 20 }}>
      {/* Contacts section moved to MeetTheTeamPage */}
    </View>
  );
}; 